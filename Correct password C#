Console.WriteLine("Put password");
            string password = Console.ReadLine();
            switch (password)
            {
            //put any password you want in case "PASSWORD"
                case "PASSWORD":
                    Console.WriteLine("Correct password");
                    break;
                default:
                    Console.WriteLine("Wrong password");
                    break;
            }
            Console.ReadLine();
